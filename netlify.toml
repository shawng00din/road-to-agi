[build]
  functions = "functions"
  publish = "."

[functions]
  node_bundler = "esbuild"

[dev]
  functions = "functions"
  publish = "."

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200 